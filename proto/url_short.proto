syntax = "proto3";

package url_short;

message URL {
    string url = 1;
}

message ID {
    string id = 1;
}

service URLShort{
    rpc Short(URL) returns (ID){}
    rpc Expand(ID) returns (URL){}
}
